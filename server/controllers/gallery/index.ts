import { deleteImages } from './deleteImages.controller';
import { getImages } from './getImages.controller';
import { saveImages } from './saveImages.controller';

export default {
    getImages,
    saveImages,
    deleteImages
}